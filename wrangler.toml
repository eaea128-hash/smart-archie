name = "cloudframe"
compatibility_date = "2026-04-28"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "."

[vars]
NODE_ENV = "production"

# Build command is set in Cloudflare Dashboard UI:
# Command: npm install --omit=dev
# Output directory: .
